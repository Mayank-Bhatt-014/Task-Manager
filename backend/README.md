# Task Manager Backend

Minimal Node.js + Express backend for the Task Manager assignment.

Endpoints:
- GET /tasks
- POST /tasks
- PATCH /tasks/:id
- DELETE /tasks/:id

Run:

Install dependencies and start server:

```bash
cd backend
npm install
npm start
```

The server listens on port 4000 by default.
