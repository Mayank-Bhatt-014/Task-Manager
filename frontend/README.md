# Task Manager Frontend

Minimal React frontend for the Task Manager assignment.

Setup:

```bash
cd frontend
npm install
npm start
```

The app expects the backend at `http://localhost:4000` by default. To override, set `REACT_APP_API_URL` environment variable.
